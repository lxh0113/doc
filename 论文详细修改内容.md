# 《基于协同编辑和AI辅助的智能云笔记系统》定稿修改内容


## 修改说明

本文档根据定稿前审查意见，逐条列出具体的修改内容。**关于"口语化表达较重"的意见**：摘要和正文中的口语化表达（如"鸡肋""踩坑""我们做了"等）是刻意保留的风格，目的是控制AIGC检测率，因此本文档中不包含口语化相关的修改。以下修改内容本身也会稍微保留一些口语化色彩，避免AI痕迹过重。

---

## 一、必须马上改（高优先级）

### 1. 论文题目统一

**涉及位置**：
- 封面（原稿第3-5行）
- 摘要页标题（原稿第63行）
- 英文摘要标题（原稿第86行）
- 目录中所有出现题目的位置
- 正文页眉
- 结论与参考文献前的题名

**当前内容**：
> 基于协同编辑和AI辅助的智能云笔记系统

**修改为**：
> 基于协同编辑和AI辅助的智能云笔记系统设计与实现

**具体操作**：
- 封面"题    目"部分：将"基于协同编辑和AI辅助的智能云笔记系统"改为"基于协同编辑和AI辅助的智能云笔记系统设计与实现"
- 摘要页标题、Abstract页标题同步修改
- 目录、页眉、结论前题目、参考文献前题目全文统一

---

### 2. 第1章章节结构调整

**涉及位置**：原稿第107-142行（第1章全部内容）

**当前结构**：
```
第1章  前  言
  1.1  项目背景与意义
    1.1.1 项目背景
    1.1.2 研究开发现状分析
    1.1.3 项目的目标和范围
    1.1.4 论文结构简介
```

**修改为**：
```
第1章  绪  论
  1.1  项目背景
  1.2  研究现状
  1.3  项目目标与范围
  1.4  论文结构
```

**具体修改**：
- 将1.1.1、1.1.2、1.1.3、1.1.4分别提升为独立的一级小节1.1、1.2、1.3、1.4
- 删除原来的"1.1 项目背景与意义"这一中间层级
- 第1章标题"前 言"改为"绪 论"
- 1.1.1"项目背景"改为"1.1 项目背景"
- 1.1.2"研究开发现状分析"改为"1.2 研究现状"
- 1.1.3"项目的目标和范围"改为"1.3 项目目标与范围"
- 1.1.4"论文结构简介"改为"1.4 论文结构"

**关于口语化表达（不做修改）**：
原稿第110-113行的"分头写、事后合并""逻辑断裂甚至内容丢失""改起来成本很高"等表达保留，不做学术化改写。

---

### 3. 补充系统总体用例图（3.2节之后新增）

**涉及位置**：原稿第595行（图3-1 角色用例图）之后，建议在3.2.1节功能概述之后插入

**当前状态**：仅有图3-1角色用例图（按Actor角色划分的用例），缺少按系统功能维度的总体用例图

**需新增内容**：

**新增图号：图3-X 系统总体用例图**

该用例图以"智能云笔记系统"作为系统边界，外部参与者包含三类角色（普通用户、协作用户、系统管理员），并在每个角色旁按功能模块分组标注用例：
- 普通用户关联的用例：注册与登录、管理个人笔记（含创建/编辑/删除/搜索）、内容互动（含点赞/收藏/评论/关注）、专栏与标签管理、浏览推荐内容
- 协作用户在普通用户全部用例基础上，额外关联：创建协作文档、加入协作编辑、管理协作权限、使用AI辅助（含纠错/润色/翻译/扩写/摘要/AI小助手）、查看版本历史
- 系统管理员关联的用例：用户管理（禁用/启用/删除）、内容审核（审核通过/驳回）、举报处理、数据统计查看、轮播图管理

用例之间的包含(include)和扩展(extend)关系：
- "使用AI辅助"包含"文本纠错""文本润色""文本翻译""文本扩写""文本摘要""AI小助手"
- "管理协作权限"扩展自"创建协作文档"
- "内容审核"扩展自"举报处理"

---

### 4. 补充协同编辑核心用例图（3.2.2节之后新增）

**涉及位置**：原稿第696-721行（UC2协同编辑用例表）之后

**需新增内容**：

**新增图号：图3-Y 协同编辑核心用例图**

该用例图聚焦于"协作用户"这一Actor，系统边界为"协同编辑子系统"。用例包含：
- **创建协作文档**：协作用户触发，前置条件是用户已登录且具有协作权限
- **邀请成员**：扩展自"创建协作文档"，由文档owner发起
- **设置成员权限**：扩展自"邀请成员"，owner设定被邀请者的角色为edit或view
- **实时编辑**：edit权限成员可执行，include关系指向"CRDT操作同步""光标位置同步"
- **只读查看**：view权限成员可执行
- **保存快照**：由系统自动触发（定时保存）或用户手动触发
- **查看版本历史**：所有协作者可执行，include关系指向"预览历史版本"
- **退出协作**：所有协作者可执行

并在图注中说明owner/edit/view三种文档级权限在上述用例中的适用范围。

---

### 5. 新增智能云笔记核心领域类图（替换现有图3-3）

**涉及位置**：原稿第877-886行（3.3对象模型分析）

**当前问题**：现有图3-3领域类图过于简单，仅包含了Article、CooperateArticle、Controller、DAO等对象，缺少系统级的核心领域对象及其关系。

**修改为**：

**保留图3-3图号，但内容重画为"智能云笔记核心领域类图"**

类图中至少包含以下类及其关系：

**(1) User（用户）**
属性：id, username, email, avatar, intro, gender, age
关系：
- User 1→* Article（一对多：一个用户发布多篇文章）
- User 1→* Comment（一对多：一个用户发表多条评论）
- User 1→* Love（一对多：一个用户多次点赞）
- User 1→* Collect（一对多：一个用户多次收藏）
- User 1→* MyColumn（一对多：一个用户创建多个专栏）
- User 1→* Label（一对多：一个用户创建多个标签）
- User 1→* CooperateMember（一对多：一个用户可以参与多个协作文档）

**(2) Article（文章）**
属性：id, title, content, status, brief, kind, date, url
关系：
- Article *→1 User（多对一：每篇文章属于一个作者）
- Article *→1 MyColumn（多对一：文章属于一个专栏）
- Article *→* Label（多对多：通过Article_Label关联表）
- Article 1→* Comment（一对多：文章有多条评论）
- Article 1→* Love（一对多：文章被多次点赞）
- Article 1→* Collect（一对多：文章被多次收藏）
- Article 1→* Browse（一对多：文章被多次浏览）
- Article *→1 Category（多对一：文章属于一个分类）

**(3) CooperateArticle（协作文档）**
属性：id, title, content, time, version, status
关系：
- CooperateArticle 1→* CooperateMember（一对多：一个协作文档有多位成员）
- CooperateArticle 1→* CooperateRevision（一对多：一个协作文档有多个版本快照）
- CooperateArticle *→1 User（多对一：created_by指向创建者）

**(4) CooperateMember（协作成员）**
属性：id, role(owner/edit/view), joined_at
关系：
- CooperateMember *→1 CooperateArticle（多对一）
- CooperateMember *→1 User（多对一）

**(5) CooperateRevision（协作版本快照）**
属性：id, revision_no, title, content, editor_id, created_at
关系：
- CooperateRevision *→1 CooperateArticle（多对一）

**(6) AICallLog（AI调用记录）**
属性：id, user_id, func_type, call_path, input_length, output_length, duration_ms, status_code, cache_hit, error_msg, created_at
关系：
- AICallLog *→1 User（多对一：记录归属于调用AI的用户）

**(7) Notification（通知消息）**
属性：id, user_id, type, trigger_user_id, article_id, comment_id, content, is_read, created_at
关系：
- Notification *→1 User（多对一：通知的接收者）
- Notification *→0..1 Article（可选关联文章）
- Notification *→0..1 Comment（可选关联评论）

**(8) RecommendResult（推荐结果缓存）**
属性：id, user_id, article_ids(JSON), algo_type, created_at, expires_at
关系：
- RecommendResult *→1 User（一对一：每个用户一条缓存记录，user_id唯一）

**(9) ArticleSimilarity（文章相似度）**
属性：article_id_1, article_id_2, similarity_score
关系：自关联

**另外，类图中应体现服务层调用关系**：
- ArticleController → ArticleDao（数据访问）
- CooperateArticleController → CooperateArticleDao / CooperateRevisionDao
- AICallController → AICallLogDao
- RecommendService → ArticleSimilarityDao / RecommendResultDao

---

### 6. 优化系统E-R图（重画图4-4）

**涉及位置**：原稿第1080-1131行（4.4.1概念设计）

**当前问题**：现有E-R图对数据库表结构的覆盖不够系统化，缺少对核心关联链的清晰表达。

**修改为**：

**重画图4-4为"系统核心E-R图"，突出以下四条数据链：**

**数据链一：用户—文章—标签/栏目—评论/点赞/收藏**
- User(1) —— 发布 —— (N)Article
- Article(N) —— 属于 —— (1)MyColumn
- Article(M) —— 关联 —— (N)Label（通过Article_Label中间表）
- Article(1) —— 收到 —— (N)Comment
- User(1) —— 发表 —— (N)Comment
- Article(1) —— 被 —— (N)Love
- User(1) —— 点赞 —— (N)Love
- Article(1) —— 被 —— (N)Collect
- User(1) —— 收藏 —— (N)Collect

**数据链二：用户—协作文档—协作成员—版本快照**
- User(1) —— 创建 —— (N)CooperateArticle
- CooperateArticle(1) —— 包含 —— (N)CooperateMember
- User(1) —— 成为 —— (N)CooperateMember
- CooperateArticle(1) —— 产生 —— (N)CooperateRevision
- User(1) —— 保存 —— (N)CooperateRevision（editor_id）

**数据链三：用户—AI调用记录—功能类型—调用路径**
- User(1) —— 触发 —— (N)AICallLog
- AICallLog中func_type字段区分为polish/translate/expand/correct/summary/chat
- AICallLog中call_path字段区分为backend_proxy/frontend_direct/frontend_ws

**数据链四：用户—互动行为—推荐结果缓存**
- User(1) —— 产生 —— (N)互动行为（Love/Collect/Comment/Browse）
- User(1) —— 拥有 —— (1)RecommendResult（user_id唯一约束）
- RecommendResult中的article_ids引用Article表

**需要特别注意的字段约束图注**：
- recommend_result.user_id标注"唯一约束（一用户一缓存记录）"
- cooperate_member中(article_id, user_id)标注"联合唯一约束（同一用户在同一文档中只能有一条成员记录）"
- ai_call_log中(user_id, created_at)标注"联合索引"
- article_similarity中(article_id_1, article_id_2)标注"联合唯一约束"

---

### 7. 数据库表结构补充索引、外键和唯一约束（4.4.2节）

**涉及位置**：原稿第1131-1970行（4.4.2逻辑设计中各表），需逐表补充约束说明。

**当前问题**：大部分表的"外键"列标记为"-"，索引和唯一约束没有明确标注。

**修改内容**：

**(1) cooperate_member表（表4-18，原稿第1657-1688行）**
在表后补充说明：
- 外键约束：article_id REFERENCES cooperate_article(id)，user_id REFERENCES user(id)
- 唯一约束：UNIQUE(article_id, user_id)，确保同一用户在同一协作文档中只有一条成员记录
- 索引：INDEX idx_cm_article (article_id)，INDEX idx_cm_user (user_id)

**(2) cooperate_article_revision表（表4-20，原稿第1736-1777行）**
补充说明：
- 外键约束：article_id REFERENCES cooperate_article(id)，editor_id REFERENCES user(id)
- 唯一约束：UNIQUE(article_id, revision_no)
- 索引：INDEX idx_car_article (article_id)

**(3) ai_call_log表（表4-21，原稿第1778-1839行）**
补充说明：
- 外键约束：user_id REFERENCES user(id)
- 索引：INDEX idx_acl_user_time (user_id, created_at)，INDEX idx_acl_func_time (func_type, created_at)，INDEX idx_acl_call_path (call_path)
- 说明：user_id和created_at的联合索引用于查询用户最近的AI调用记录；func_type和created_at的联合索引用于按功能类型统计调用情况

**(4) recommend_result表（表4-24，原稿第1934-1970行）**
补充说明：
- 外键约束：user_id REFERENCES user(id)
- 唯一约束：UNIQUE(user_id)，确保每个用户只有一条推荐缓存记录
- 索引：INDEX idx_rr_expires (expires_at)，用于定时任务清理过期记录

**(5) notification表（表4-23，原稿第1882-1933行）**
补充说明：
- 外键约束：user_id REFERENCES user(id)，trigger_user_id REFERENCES user(id)，article_id REFERENCES article(id)，comment_id REFERENCES comment(id)
- 索引：INDEX idx_notif_user_read (user_id, is_read)，INDEX idx_notif_created (created_at)

**(6) article_audit_log表（表4-22，原稿第1840-1881行）**
补充说明：
- 外键约束：article_id REFERENCES article(id)，admin_id REFERENCES user(id)
- 索引：INDEX idx_audit_article (article_id)，INDEX idx_audit_admin (admin_id)

**(7) cooperate_invitation表（表4-19，原稿第1689-1735行）**
补充说明：
- 外键约束：article_id REFERENCES cooperate_article(id)，inviter_id REFERENCES user(id)，invitee_id REFERENCES user(id)
- 索引：INDEX idx_ci_invitee_status (invitee_id, status)

---

### 8. 补充协同编辑专项测试（6.3节）

**涉及位置**：原稿第2866-2965行（已有CT-01至CT-12的协同编辑测试用例表）

**当前状态**：已有12个协同编辑测试用例（CT-01至CT-12，从第2887行开始），内容较完整，覆盖了并发插入、删除插入、延迟加入、Saver选举、断线重连、版本快照、权限控制、光标同步等场景。

**需补充的内容（在表6-3中增加以下测试用例）**：

**CT-13：三用户并发插入不同位置文本**
- 测试目的：验证三位用户在不同位置同时插入文本时CRDT合并的正确性
- 前置条件：用户A、B、C同时打开同一篇协作文档（约500字）
- 操作步骤：(1)用户A在文档开头插入一段100字引言 (2)用户B同时在文档中间插入一段50字说明 (3)用户C同时在文档末尾插入一段80字结语
- 预期结果：三位用户最终看到完全一致的文档内容，三个插入片段均正确出现在各自目标位置，无覆盖、无丢失、无重复

**CT-14：编辑冲突后手动保存的一致性验证**
- 测试目的：验证发生并发编辑冲突后，手动保存的内容与屏幕显示完全一致
- 前置条件：用户A和B同时编辑同一文档
- 操作步骤：(1)A在段落1大量修改 (2)B同时在段落3大量修改 (3)等同步完成后，A和B分别点击"保存"按钮 (4)两人分别刷新页面重新加载
- 预期结果：两人重新加载后看到的内容与保存前完全一致，cooperate_article_revision表中新增两条快照记录

**CT-15：协作文档自动保存内容正确性**
- 测试目的：验证Saver自动保存机制正确持久化合并后的文档内容
- 前置条件：用户A、B在线编辑，A为Saver（ID最小者）
- 操作步骤：(1)A和B分别在不同段落进行编辑 (2)等待30秒让自动保存触发 (3)关闭所有浏览器窗口 (4)A重新打开文档
- 预期结果：重新打开后内容与自动保存时合并后的状态一致，所有编辑内容均已正确持久化

**CT-16：用户退出协作后再加入内容同步**
- 测试目的：验证用户短暂退出后重新加入协作的同步完整性
- 前置条件：用户A在线编辑文档
- 操作步骤：(1)A编辑文档约200字修改 (2)A关闭浏览器 (3)A在5分钟后通过原协作链接重新打开文档
- 预期结果：A重新打开后看到的内容为最新版本（包括A退出期间其他协作者可能做出的修改），且A退出前未保存的本地修改在重连后自动同步

---

### 9. AI前端直连安全表述修改（2.2.3节 + 2.2.4节）

**涉及位置1**：原稿第308-309行（综合风险评估）

**当前内容**：
> 前端直连路径虽然暴露平台级API Key，但结合域名白名单与CSP策略，且处理的是用户主动提交的非敏感文本，整体风险可控。相比之下，该架构在降低后端负载、减少延迟、实现端到端流式传输方面的技术收益更明显。

**修改为**：
> 当前版本中，前端直连路径采用了域名白名单和CSP策略进行风险缓解，在开发测试环境下处理用户主动提交的非敏感文本，风险和收益的权衡是可以接受的。但需要指出的是，这种方案在生产环境里仍然存在API Key被恶意提取并滥用的风险，不能简单认为"风险可控即可上线"。如果将来要面向公网部署，建议改为以下方案之一：(1)通过后端转发SSE流，由后端代理服务统一管理API密钥、做调用审计和限流；(2)如果必须保留前端直连以降低延迟，则改用短期临时令牌机制——前端每次调用前先向后端申请一个有效期为5分钟的一次性令牌，第三方API仅接受由后端签发的临时令牌。

**涉及位置2**：原稿第331-334行（API密钥安全设计中的"公开级密钥"描述）

**当前内容**：
> 公开级密钥（前端路径）：Elysia Tools的API Key不能走后端代理（走了就会破坏SSE直连的低延迟优势）。我在服务商后台开了域名白名单，这样密钥就算被盗也只有在授权域名下才能调用。

**修改为**：
> 公开级密钥（前端路径）：当前版本中，为了保持SSE直连的低延迟特性，Elysia Tools的API Key配置在前端代码中。缓解措施是在服务商后台启用了域名白名单（CORS Origin限制），使该Key仅能在本系统授权的域名下被调用。需要说明的是，这是开发/实验环境的临时方案——在生产环境中，建议将扩写和总结功能也迁入后端代理，或者在Elysia Tools支持的前提下，采用短期临时令牌（前端先向后端请求一个5分钟有效的临时token，再用这个token直连第三方API）。

**涉及位置3**：第7章结论中"不足"部分（原稿第3170-3174行）需要补充

在原稿第3174行后增加一点不足：
> （X）前端直连API Key存在生产级安全隐患。当前扩写和总结功能采用前端直连Elysia Tools的SSE端点，虽然开发测试阶段通过域名白名单控制了风险，但若面向公网部署，API Key暴露在前端仍是需要优先解决的安全问题。

---

### 10. 第5章突出核心代码闭环（5.2节、5.3节重点修改）

**涉及位置1**：原稿第2066-2248行（5.2协作编辑模块）

需在现有内容基础上，在5.2.6节之后（约第2248行后），**新增以下一个小节**：

**新增5.2.7 协作成员权限校验**

正文：
系统在用户加入协作编辑时，根据cooperate_member表中该用户对应当前文档的role字段，在服务端和前端两个层面进行权限校验。

服务端权限校验逻辑（y-websocket连接阶段）：
```
// 伪代码：y-websocket服务端连接鉴权
function onConnection(ws, req) {
  const token = parseToken(req.url);        // 从URL参数解析JWT
  const userId = verifyToken(token);        // 验证token，提取用户ID
  const docId = parseDocId(req.url);        // 提取文档ID
  const member = db.query(
    "SELECT role FROM cooperate_member WHERE article_id = ? AND user_id = ?",
    [docId, userId]
  );
  if (!member) {
    ws.close(4003, "无协作权限");           // 非协作者拒绝连接
    return;
  }
  // view权限用户建立连接，但标记为只读
  if (member.role === 'view') {
    ws.readOnly = true;
  }
}

function onMessage(ws, message) {
  if (ws.readOnly && message.type === 'edit') {
    // view用户尝试编辑，拒绝并返回提示
    ws.send({ type: 'error', msg: '您当前为只读权限，无法编辑' });
    return;
  }
  // 正常处理edit权限用户的编辑操作
}
```

前端权限校验逻辑：
- 前端根据cooperateArticle.role字段控制界面状态：
  - role = 'owner' 或 'edit'：编辑器可编辑，工具栏全部可用
  - role = 'view'：编辑器设为readOnly模式，工具栏中编辑相关按钮置灰，页面顶部显示"只读模式"标签
  - 非协作者：无法进入协作页面，跳转回首页并提示"您没有此文档的访问权限"

**新增5.2.8 版本快照保存与回滚**

在现有"定时自动保存"（5.2.6.1）和"服务端状态恢复"（5.2.6.2）的基础上，补充版本快照的保存触发条件和版本回滚流程：

快照保存触发条件：
1. 用户手动点击"保存"按钮时，立即生成一条快照记录
2. 自动保存（Saver选举机制）每30秒检查并保存，同时写入快照
3. 快照数量上限为50条，超出时自动删除最旧的快照（revision_no最小者）

版本回滚流程：
- 用户在"版本历史"列表中定位目标版本
- 点击"恢复到此版本"按钮
- 前端弹出确认对话框，提醒用户"恢复操作会覆盖当前内容，建议先手动保存当前版本"
- 确认后，后端执行：(1)将当前文档状态作为新快照保存（记录回滚前状态）(2)将目标版本的内容写入cooperate_article.content (3)Y.Doc重新加载更新后的内容 (4)通过WebSocket广播文档已更新的通知给所有在线协作者
- 前端收到通知后自动刷新编辑器内容

---

**涉及位置2**：原稿第2249-2382行（5.3 AI辅助模块）

需在5.3.4节（核心功能实现）之后，补充**AI调用日志记录和SSE异常处理的完整实现**：

**新增5.3.5 AI调用日志记录**

正文：
系统在三条AI调用路径上分别记录调用日志到ai_call_log表。

后端代理路径（纠错、润色、翻译）的日志记录（在Python Flask服务的每个API入口记录）：
```
@app.route('/api/ai/correct', methods=['POST'])
def correct():
    user_id = request.json.get('user_id')
    text = request.json.get('text', '')
    start_time = time.time()
    try:
        result = xunfei_client.correct(text)
        duration = int((time.time() - start_time) * 1000)
        # 记录成功日志
        log_ai_call(user_id, 'correct', 'backend_proxy',
                    len(text), len(result), duration, 200, cache_hit=0)
        return jsonify(code=0, data=result)
    except Exception as e:
        duration = int((time.time() - start_time) * 1000)
        log_ai_call(user_id, 'correct', 'backend_proxy',
                    len(text), 0, duration, 500, cache_hit=0, error=str(e))
        return jsonify(code=500, msg='服务繁忙，请稍后重试')
```

前端直连路径（扩写、总结）的埋点记录：
- 前端在SSE连接成功建立并收到第一块数据后，发一个埋点请求POST /api/ai/log到后端
- 埋点请求参数：{ func_type: 'expand'|'summary', call_path: 'frontend_direct', input_length: N }
- SSE连接关闭时（正常结束或异常中断），前端再发一个埋点请求更新output_length和duration_ms

前端WebSocket路径（AI小助手）的埋点：
- 在WebSocket onopen时发送开始埋点
- 在onclose时发送结束埋点，携带总duration和总output_length

**新增5.3.6 SSE流式中断与重试处理**

正文：
前端SSE直连路径（扩写和总结）的关键容错处理：

```
async function connectSSE(url, body, onChunk, onDone, onError) {
  try {
    const response = await fetch(url, {
      method: 'POST',
      headers: { 'Content-Type': 'application/json', 'Authorization': `Bearer ${API_KEY}` },
      body: JSON.stringify(body)
    });

    if (!response.ok) {
      onError(`请求失败 (${response.status})`);
      return;
    }

    const reader = response.body.getReader();
    const decoder = new TextDecoder();
    let fullContent = '';

    while (true) {
      const { done, value } = await reader.read();
      if (done) {
        onDone(fullContent);
        break;
      }

      const chunk = decoder.decode(value, { stream: true });
      // 解析SSE格式：data: {...}
      const lines = chunk.split('\n');
      for (const line of lines) {
        if (line.startsWith('data: ')) {
          const data = JSON.parse(line.slice(6));
          if (data.type === 'done') {
            onDone(fullContent);
            return;
          }
          if (data.chunk) {
            fullContent += data.chunk;
            onChunk(fullContent);  // 实时更新界面
          }
        }
      }
    }
  } catch (error) {
    // 网络中断时保留已生成的内容，提示用户
    onError(fullContent ? `生成中断，已保留${fullContent.length}字内容` : '连接失败，请重试');
  }
}
```

关键容错点：
- 网络中断时保留已生成的文本内容，不丢弃
- 对HTTP非200响应给出明确的错误提示
- 用户可以在中断后重新点击触发，重新生成

---

### 11. 检查全文图号、表号、图名、正文引用是否一致

**涉及位置**：全文

**需检查以下内容**：

(1) 图号连续性检查：
- 第2章：图2-1、图2-2、图2-3
- 第3章：图3-1、图3-2、图3-3、图3-4、图3-5
- 第4章：图4-1、图4-2、图4-3、图4-4
- 第5章：图5-1至图5-17
- 第6章：图6-1
- 新增的图（系统总体用例图、协同编辑核心用例图、核心领域类图、部署架构图等）需要重新排号，确保编号连续

(2) 表号连续性检查：
- 第2章：表2-1至表2-6
- 第3章：表3-1至表3-11
- 第4章：表4-1至表4-24
- 第5章：表5-1至表5-2
- 第6章：表6-1至表6-7
- 新增的对应表需要重新排号

(3) 正文引用检查：
- 在正文中搜索所有"如图X-X""如表X-X"的引用，确保每个引用对应的图和表确实存在
- 特别检查新增图表和重新编号后的图表引用是否正确

(4) 图名/表名检查：
- 每张图下方应有"图X-X XXX图"的标题
- 每张表上方应有"表X-X XXX表"的标题
- 检查图名和表名是否与目录一致

---

## 二、建议尽量改（中高优先级）

### 12. 新增部署架构图（第4章或第6章）

**涉及位置**：建议在4.1.3节技术架构图（图4-2，原稿第1064-1065行）之后新增

**需新增内容**：

**新增图号：图4-X 系统部署架构图**

部署架构图应区分以下部署节点和通信关系：

```
┌─────────────────────────────────────────────────────┐
│                    用户终端                          │
│   Chrome / Firefox / Edge / Safari                  │
│   https://cloudnote.example.com                     │
└──────────────┬──────────────────────────────────────┘
               │ HTTPS (443)
┌──────────────▼──────────────────────────────────────┐
│              Nginx 反向代理                         │
│   - HTTPS终止 (SSL证书)                             │
│   - 静态资源服务 (React build)                      │
│   - 反向代理路由:                                    │
│     /api/*    → Spring Boot (8080)                  │
│     /ws/*     → Node.js y-websocket (1234)          │
│     /ai/*     → Python Flask (5000)                 │
└──┬──────────────┬──────────────┬────────────────────┘
   │ HTTP         │ WebSocket    │ HTTP
   │ (8080)       │ (1234)       │ (5000)
   ▼              ▼              ▼
┌──────────┐ ┌──────────┐ ┌──────────────┐
│ Spring   │ │ Node.js  │ │ Python Flask │
│ Boot     │ │ y-web-   │ │ AI代理服务   │
│ 核心业务 │ │ socket   │ │              │
│ 服务     │ │ 协同服务 │ │              │
│          │ │          │ │              │
│ :8080    │ │ :1234    │ │ :5000        │
└───┬──────┘ └──────────┘ └──────┬───────┘
    │                             │
    │ MySQL TCP (3306)            │ HTTPS (443)
    ▼                             ▼
┌──────────┐              ┌──────────────┐
│ MySQL    │              │ 第三方AI服务 │
│ 8.0      │              │ 讯飞API      │
│ Redis    │              │ Elysia Tools │
│ OSS      │              │ 讯飞星火     │
└──────────┘              └──────────────┘
```

图注中说明：
- Nginx作为唯一入口网关，负责HTTPS加密和请求路由
- Spring Boot核心业务服务处理业务逻辑，监听8080端口
- Python Flask AI代理服务部署在内网，仅通过Spring Boot转发访问，不直接暴露给外网
- Node.js y-websocket协同服务处理WebSocket长连接，监听1234端口
- MySQL、Redis、OSS部署在同一内网，不暴露公网端口
- 第三方AI服务通过公网HTTPS调用

---

### 13. 新增AI辅助时序图（区分两条路径）

**涉及位置**：建议在5.3.1节或5.3.2节中新增

**需新增内容**：

**新增图号：图5-X AI辅助双路径时序图**

分为两个子图：

**子图A：后端代理路径（纠错/润色/翻译）时序图**
参与者：用户、React前端、Spring Boot后端、Python Flask AI服务、讯飞API

交互流程：
1. 用户在编辑器中选中文本，点击AI功能按钮（纠错/润色/翻译）
2. 前端发送POST请求到Spring Boot后端 /api/ai/{func_type}
3. Spring Boot进行权限校验（JWT验证 + 协作用户身份确认）
4. Spring Boot进行限流检查（Redis计数器）
5. Spring Boot转发请求到Python Flask AI服务 http://localhost:5000/api/ai/{func_type}
6. Flask服务检查本地缓存（MD5哈希匹配）
7. 缓存命中：直接返回缓存结果
8. 缓存未命中：封装请求调用讯飞开放平台API
9. 讯飞API返回处理结果
10. Flask服务将结果格式化并写入缓存
11. Flask服务记录AI调用日志到ai_call_log表
12. Flask服务返回结果给Spring Boot
13. Spring Boot返回结果给前端
14. 前端Markdown渲染后展示给用户
15. 用户确认后，替换编辑器中的选中文本

异常路径（虚线标注）：
- 步骤7a：Flask服务调用讯飞API超时（>5秒），返回code=500"服务繁忙"
- 步骤7b：Flask服务宕机，Spring Boot健康检查发现不可用，直接返回"AI功能维护中"

**子图B：前端直连路径（扩写/总结SSE）时序图**
参与者：用户、React前端、Elysia Tools API

交互流程：
1. 用户在编辑器中选中文本，点击"扩写"或"总结"
2. 前端发起HTTPS POST请求到 https://elysiatools.com/api/expand（或summarize）
3. Elysia Tools通过CORS Origin验证域名白名单
4. Elysia Tools开始流式返回SSE数据块
5. 前端onmessage逐块接收，实时拼接并刷新展示区域
6. 前端发送埋点请求POST /api/ai/log到后端（记录调用开始）
7. SSE返回done信号，流式传输结束
8. 前端发送埋点请求（记录完成状态、总输出长度和耗时）
9. 用户确认后，一键替换编辑器中的选中文本

异常路径（虚线标注）：
- 步骤4a：SSE连接中断（网络抖动），前端onerror触发，保留已生成的文本内容，显示"生成中断"提示
- 步骤4b：Elysia Tools返回HTTP 429（限流），前端提示"请求太频繁，请稍后重试"

---

### 14. 新增协同编辑活动图（带泳道）

**涉及位置**：建议放在5.2.4节或5.2.5节之后

**新增图号：图5-Y 协同编辑活动图（泳道图）**

泳道划分：用户A、用户B、前端编辑器、y-websocket服务、数据库

活动流程：
1.【用户A】打开协作文档 → 
2.【前端编辑器】建立WebSocket连接(携带JWT token) → 
3.【y-websocket服务】验证token + 查询cooperate_member表确认权限 →
4.【y-websocket服务】权限不足 → 拒绝连接，返回提示
5.【y-websocket服务】权限ok → 从数据库加载Y.Doc初始状态 →
6.【数据库】返回文档内容 →
7.【y-websocket服务】初始化Y.Doc，通知前端synced →
8.【前端编辑器】渲染文档内容，编辑器就绪 →
9.【用户A】编辑文本 →
10.【前端编辑器】Yjs捕获操作，生成Y.Event，通过WebSocket发送 →
11.【y-websocket服务】接收操作，合并到Y.Doc，广播给所有在线客户端 →
12.【前端编辑器(用户B)】接收广播，Yjs合并远程操作到本地Y.Doc →
13.【前端编辑器(用户B)】Quill编辑器视图更新，显示用户A的修改 →
14.【用户B】看到实时更新 →

分支1：定时保存（虚线路径）
→ 【前端编辑器】Saver选举（在线用户ID最小者）→ Saver每30秒触发保存 → 
【y-websocket服务】writeState写入cooperate_article_revision快照 → 【数据库】持久化

分支2：用户主动保存（虚线路径）
→ 【用户A/B】点击保存按钮 → 【前端编辑器】调用saveAPI → 
【数据库】更新cooperate_article.content + 写入cooperate_article_revision新快照

分支3：断线重连（虚线路径）
→ 【前端编辑器】WebSocket onclose触发 → 自动重连（最多3次）→ 
重连成功 → 重新同步Y.Doc状态 → 恢复编辑
重连失败 → 提示"连接已断开，请刷新页面"

分支4：只读用户被拒绝编辑（虚线路径）
→ 【用户B(view权限)】尝试编辑 → 【前端编辑器】检测到role=view → 
编辑器保持readOnly状态，提示"只读模式，无法编辑"

---

### 15. 新增AI辅助活动图（带泳道）

**涉及位置**：建议放在5.3.3节或5.3.4节之后

**新增图号：图5-Z AI辅助活动图（泳道图）**

泳道划分：用户、前端、Spring Boot后端、Python Flask服务、第三方AI API

活动流程：
【用户】在编辑器中选中一段文本 →
【用户】点击AI功能按钮，弹出功能菜单（纠错/润色/翻译/扩写/摘要）→

─ 分流判断：功能类型？ ─

→ 分支A：纠错/润色/翻译（后端代理路径）
  【前端】发送POST /api/ai/{type}到后端，携带选中文本和用户token →
  【Spring Boot后端】JWT身份验证 → 验证失败 → 返回"请先登录"
  【Spring Boot后端】验证通过 → Redis限流检查 → 超限 → 返回429"操作太频繁"
  【Spring Boot后端】限流ok → 转发到Python Flask →
  【Python Flask服务】检查本地缓存(MD5) → 命中 → 直接返回
  【Python Flask服务】缓存未命中 → 封装鉴权参数，调用讯飞API →
  【第三方AI API】返回处理结果 →
  【Python Flask服务】格式化结果，写入缓存，记录ai_call_log →
  【Spring Boot后端】返回结果给前端 →
  【前端】Markdown渲染，展示给用户 →

→ 分支B：扩写/摘要（前端直连SSE路径）
  【前端】直连Elysia Tools SSE端点 →
  【第三方AI API】验证Origin域名白名单 → 验证失败 → 返回403
  【第三方AI API】验证通过 → 开始流式返回SSE分片 →
  【前端】逐块接收并拼接，实时刷新展示区域 →
  流式传输中网络中断 → 【前端】保留已生成内容，提示"生成中断，可重试"
  流式正常结束 →

→ 合并 →
【用户】查看AI处理结果 →
【用户】满意？ → 满意 → 点击"替换选中文本" → 【前端】将结果回填到编辑器
【用户】不满意 → 关闭面板 / 再次触发

---

### 16. 增加"需求—功能模块—测试用例"对应表（第6章）

**涉及位置**：在原稿第6章6.3节开头（约第2629-2631行），在现有测试用例表之前插入

**需新增内容**：

**新增表6-X 需求—功能模块—测试用例对应表**

| 需求编号 | 功能模块 | 对应章节 | 核心测试用例 | 测试类型 |
|---------|---------|---------|------------|---------|
| UC1 管理个人笔记 | 文章编辑模块 | 5.1 | FT-04文章草稿保存, FT-05文章草稿更新, FT-06文章发布, FT-07/08文章列表, FT-09文章搜索, FT-10文章详情 | 功能测试 |
| UC2 协同编辑 | 协作编辑模块 | 5.2 | FT-11至FT-19 + CT-01至CT-16（协同编辑专项） | 功能测试 + 专项测试 |
| UC3 使用AI辅助 | AI辅助模块 | 5.3 | AI-01至AI-15 + AS-01至AS-10（AI功能与安全） | 功能测试 + 安全测试 |
| UC4 内容互动 | 消息系统模块 | 5.4 | FT-20至FT-23（消息/私信） | 功能测试 |
| UC5 专栏标签管理 | 文章编辑模块 | 5.1 | （含在FT-04/05中，编辑时设置专栏/标签） | 功能测试 |
| UC6 推荐系统 | 推荐算法 | 2.4 | 需补充推荐算法专项测试 | 算法评估 |
| UC7 系统管理 | 后台文章管理模块 | 5.5 | FT-24后台文章查询, FT-25后台审核通过/驳回 | 功能测试 |
| 性能需求 | 全系统 | - | 表6-5核心业务性能测试 + 表6-6协同编辑专项性能测试 | 性能测试 |
| 安全需求 | 全系统 | - | AS-01至AS-10 | 安全测试 |

---

### 17. 压缩推荐算法篇幅，突出辅助定位

**涉及位置**：原稿第372-478行（2.4个性化推荐算法）

**当前问题**：推荐算法有较完整的公式推导、评估指标和流程图，篇幅较长，但题目核心是"协同编辑+AI辅助"

**修改建议**：
- **保留**：Item-CF核心思想（2.4.2的第一段，原稿第382-383行）、权重矩阵表（表2-5）、评估结果表（表2-6）、总体流程图（图2-3）
- **保留**：冷启动策略的核心描述（2.4.3，原稿第433-441行），但精简冷启动公式的详细展开
- **压缩**：三个公式（2.1、2.2、2.3）的详细推导过程可简化，保留公式本身但减少对每个符号的解释篇幅
- **压缩**：2.4.5节（工程实现要点，原稿第471-477行）可大幅精简，将定时预计算和增量更新的细节移到第6章测试部分或第7章改进方向中
- **整体压缩目标**：2.4节从当前约6页压缩到约4页

**具体操作**：
- 原稿第383-427行的详细公式推导（第一步至第三步），各步保留公式和一句话解释，删除逐行符号说明
- 原稿第428-432行"计算复杂度分析"保留但精简为一句话
- 原稿第442-468行"推荐效果离线评估"，保留评估结果表（表2-6），评估指标定义（2.5-2.7公式）精简
- 2.4.5节从约30行精简到约10行

---

### 18. 补充SSE中断、AI超时、限流、缓存命中等测试用例

**涉及位置**：原稿第2789-2865行（表6-2 AI辅助功能及异常场景测试用例）

**当前状态**：已有AI-01至AI-15，覆盖了AI对话、文本处理、异常场景和并发测试，内容较完整

**需补充的测试用例**：

**在表6-2中增加：**

**AI-16：翻译缓存命中验证**
- 测试目的：验证翻译结果缓存正确命中且返回更快
- 输入数据：同一段中文文本
- 操作步骤：(1)第一次调用翻译API，记录响应时间T1和cache_hit=0 (2)30分钟后对完全相同文本再次调用翻译API
- 预期结果：第二次响应时间T2 < T1；ai_call_log中第二次调用的cache_hit=1；翻译结果内容与第一次一致

**AI-17：用户级限流精确触发**
- 测试目的：验证限流计数的精确性
- 输入数据：已登录用户token
- 操作步骤：(1)在60秒内连续调用纠错API 11次（限流阈值为10次/分钟）(2)观察第11次请求的响应
- 预期结果：前10次正常返回；第11次返回HTTP 429，响应体包含"操作太频繁，请稍后再试"；第11次不计入ai_call_log的success统计

**AI-18：全局限流保护触发**
- 测试目的：验证全局QPS=50限流是否正常触发
- 输入数据：JMeter 60线程并发
- 操作步骤：使用JMeter模拟60个并发用户同时调用纠错API，持续10秒
- 预期结果：部分请求返回HTTP 429降级码；无请求返回5xx错误；Python AI代理服务进程未崩溃

**AI-19：敏感词过滤验证**
- 测试目的：验证后端代理路径的敏感词过滤功能
- 输入数据：包含敏感词汇的测试文本
- 操作步骤：提交包含预设敏感词的文本进行翻译
- 预期结果：请求被拦截，返回提示"内容包含违规信息，请修改后重试"，不调用外部API

---

### 19. 第7章结论按"已完成工作—存在不足—后续改进"规范

**涉及位置**：原稿第3156-3210行（第7章全部）

**当前状态**：已有7.1工作总结（5点）、7.2存在不足（4点）、7.3后续改进方向（4点），结构合理

**修改内容**：

**(1) 7.1 工作总结（原稿第3158-3168行）**

当前的5点总结改为按五个维度归纳：

> 本项目按照"需求分析→架构设计→模块实现→系统测试"的工程流程，完成了融合协同编辑和大模型辅助的智能云笔记系统的全栈开发。主要工作按五个维度归纳如下：
>
> **基础云笔记**：实现了用户登录认证（JWT）、栏目-标签两级分类体系、文章从草稿到发布到归档的完整生命周期管理、基于MySQL LIKE的全文搜索，以及点赞、收藏、评论、关注等社交互动功能。
>
> **协同编辑**：基于Yjs的CRDT（YATA算法）和y-websocket传输层，构建了多人实时无锁协同编辑通道。关键能力包括：操作增量的确定性广播与自动冲突消解、Awareness协议驱动的光标位置实时感知、Saver选举机制的定时自动保存、版本快照的自动创建与50条上限裁剪、以及基于cooperate_member表的owner/edit/view三级文档权限控制。
>
> **AI辅助**：按安全敏感度和延迟要求设计了双路径AI接入架构：后端代理路径（纠错、润色、翻译，经Python Flask微服务统一管理密钥和审计日志）和前端直连路径（扩写、总结，经SSE流式接口减少延迟）。集成了讯飞开放平台和Elysia Tools，提供校对、润色、翻译、扩写、摘要和AI小助手六项功能。
>
> **推荐系统**：基于物品协同过滤（Item-CF）实现了首页和侧边栏的个性化文章推荐，结合热门推荐和随机曝光策略处理用户和文章的冷启动问题。离线评估显示Item-CF在准确率（72.3%）、覆盖率（68.5%）和多样性（0.74）上均显著优于热门推荐基线。
>
> **测试验证与容器化部署**：使用Apache JMeter在100用户并发场景下验证了核心接口平均响应时间控制在250ms以内、协同编辑同步延迟中位数在50ms以下的设计目标。通过Docker Compose实现了Spring Boot、Flask、Node.js、MySQL、Redis等六大服务的一键容器化部署。

**(2) 7.2 存在不足（原稿第3169-3181行）**

在原有4点不足基础上，补充第5点：

> （5）前端直连API Key存在生产级安全隐患。当前版本中扩写和总结功能采用前端直连Elysia Tools的SSE推理端点，虽然开发测试阶段通过域名白名单控制了密钥滥用风险，但如果面向公网大规模部署，API Key在浏览器端暴露仍是需要优先解决的安全问题。下一步需要将前端直连路径迁入后端统一网关，或采用短期临时令牌机制。

**(3) 7.3 后续改进方向（原稿第3182-3210行）**

在原有4个改进方向基础上，补充第5点：

> （5）移动端适配与跨平台扩展
> 当前系统主要面向桌面浏览器设计，移动端仅做了响应式适配的基本处理。后续可以基于React Native或Flutter开发独立的移动客户端，实现以下能力：离线笔记编辑（本地CRDT状态缓存，联网后自动合并）、拍照OCR识别转笔记、语音速记转文字、移动端推送通知。

---

### 20. 推荐算法增加"需求—功能模块—数据库表—测试用例"对应表

**涉及位置**：建议在4.1.2节（核心设计映射）的表4-2基础上扩展

**原稿已有**：表4-2 核心需求-功能-技术-数据映射表（原稿第1020-1040行），已经包含了UC2/UC3/UC1/UC7的映射

**需修改**：扩展表4-2，增加"测试用例"列和"推荐算法"行：

在表4-2中增加以下行：

| 业务需求 (Ch.3) | 核心功能模块 (Ch.5) | 关键技术选型 (Ch.2) | 核心数据模型 (Ch.4.4) | 关键测试用例 (Ch.6) |
|---|---|---|---|---|
| UC6: 推荐系统 | 2.4推荐算法 + Common.getRecommendArticles() | Item-CF, 冷启动策略, 定时预计算 | Recommend_Result, Article_Similarity, Browse, Love, Collect | CT-16/17/18(推荐专项待补充) |
| 协同编辑非功能需求(3.4节) | 5.2协作编辑 | y-websocket, awareness, Saver选举 | Cooperate_Article, Cooperate_Revision | CT-01至CT-16, 表6-6性能测试 |

---

## 三、图表处理清单及具体修改

### 21. 系统总体用例图（必须新增）
见上文"一-3"节，插入位置：3.2.1节之后

### 22. 协同编辑核心用例图（必须新增）
见上文"一-4"节，插入位置：3.2.2节UC2用例之后

### 23. AI辅助用例图（建议新增）

**新增图号：图3-Z AI辅助用例图**

用例包含：
- 主要Actor：协作用户
- 系统边界：AI辅助子系统
- 用例：文本纠错、文本润色、文本翻译、文本扩写、文本摘要、AI小助手
- 所有用例include"用户确认结果"
- "文本纠错""文本润色""文本翻译"include"后端代理调用"
- "文本扩写""文本摘要"include"SSE流式接收"
- "AI小助手"include"WebSocket对话"

### 24. 核心领域类图（必须重画）
见上文"一-5"节，替换现有图3-3

### 25. 协同编辑时序图（保留并修改）
原图4-3（原稿第1068-1076行）保留主流程，增加以下异常分支：
- 异常分支1（连接失败）：协作者A打开文档 → WebSocket连接失败 → 提示"连接服务器失败，请检查网络" → 提供"重试"按钮
- 异常分支2（权限不足）：协作者B通过链接打开 → WebSocket连接 → 服务端验证 → cooperate_member表中无此用户 → 拒绝连接(4003) → 前端显示"您没有此文档的访问权限"
- 异常分支3（断线重连）：编辑过程中WebSocket断开 → onclose触发 → 自动重连（指数退避，最多3次，间隔1s/2s/4s）→ 重连成功 → 同步最新状态 → 继续编辑
- 异常分支4（只读用户编辑被拒）：view权限用户尝试编辑 → 服务端拦截 → 返回权限错误 → 前端编辑器保持readOnly

### 26. AI辅助时序图（建议新增）
见上文"二-13"节，插入位置：5.3.1节

### 27. 活动图（建议新增）
见上文"二-14"（协同编辑活动图）和"二-15"（AI辅助活动图）

### 28. DFD数据流图（保留）
原稿图3-4和图3-5（原稿第957-976行）保留，正文中在图题和说明中明确标注"数据流图（DFD）"，并在第3.6节开头增加一句说明：

在第3.6节（原稿第954行）开头增加：
> 数据流图（DFD）用于从数据传递和加工的角度表达系统的逻辑功能，侧重数据流向而非控制流或时间顺序。本节使用DFD进行顶层建模，不替代第4章和第5章中用于表达交互顺序和对象协作的UML时序图、活动图和类图。

### 29. 系统E-R图（必须优化）
见上文"一-6"节，重画图4-4

### 30. 数据库表结构表（保留但校对）
见上文"一-7"节，补充主键、外键、索引、唯一约束

### 31. 技术架构图（保留）
原图4-2（原稿第1064-1065行）保留

### 32. 功能模块图（保留但优化）
原图3-2（原稿第597-598行）保留，但将图中的"创新功能模块"下的"协同编辑"和"AI辅助"用加深色块或加粗边框突出显示，并在图注中说明：

在原稿第598行图注后补充：
> 图中"协同编辑"和"AI辅助"作为本题目核心创新点，在模块结构图中以加深色块突出标注。推荐系统和社交互动（点赞、收藏、评论、关注、私信）作为辅助功能模块，与两个核心模块区分层级。

---

## 四、逐章其他修改要点

### 第3章补充

**3.4节非功能需求补充**（原稿第887-917行）：

在原稿第895-898行"并发支持"部分之后，增加以下内容：

> **协同延迟指标**：
> - 协同编辑操作（单次插入/删除）的端到端同步延迟中位数不超过50ms
> - 光标位置同步延迟不超过200ms
> - 文档初始加载（含历史状态恢复）在2秒以内完成
>
> **AI调用超时与降级**：
> - 后端代理路径（纠错/润色/翻译）：外部API调用超时上限5秒，超时后返回降级响应
> - 前端直连路径（扩写/总结SSE）：8秒内未收到首字节则提示超时
> - AI小助手WebSocket：心跳间隔30秒，3次无心跳则自动重连
>
> **WebSocket重连与容错**：
> - WebSocket断线后自动重连，指数退避策略（1s/2s/4s），最多重试3次
> - 重连成功后自动重新同步Y.Doc状态
>
> **安全控制指标**：
> - JWT Token有效期7天，过期后强制重新登录
> - 协同编辑连接在upgrade阶段完成JWT验证
> - view权限用户不能发送编辑操作，服务端层面拦截
> - 敏感数据（密码）BCrypt加盐哈希存储，传输层HTTPS加密

### 第4章补充

**4.2.1节协同编辑时序图异常分支补充**：
见上文"三-25"节

---

## 五、修改操作清单（按文件操作顺序）

考虑到操作的便利性，以下按在文档中出现的顺序排列所有需要修改的位置：

| 序号 | 位置（原稿行号） | 操作类型 | 修改内容概述 |
|-----|---------------|---------|------------|
| 1 | 第3-5行（封面题目） | 修改文字 | 题目加上"设计与实现" |
| 2 | 第25-62行（目录） | 更新目录 | 更新所有题目、章节编号、图表编号 |
| 3 | 第63行（摘要标题） | 修改文字 | 题目加上"设计与实现" |
| 4 | 第86行（Abstract标题） | 修改文字 | 题目加上"设计与实现" |
| 5 | 第107行 | 修改标题 | "前 言"改"绪 论" |
| 6 | 第108-142行 | 结构调整 | 1.1.1→1.1, 1.1.2→1.2, 1.1.3→1.3, 1.1.4→1.4 |
| 7 | 第308-309行 | 修改文字 | AI前端直连安全表述（见一-9） |
| 8 | 第331-334行 | 修改文字 | 公开级密钥描述（见一-9） |
| 9 | 第383-427行 | 精简 | 推荐算法公式推导精简 |
| 10 | 第428-432行 | 精简 | 计算复杂度分析精简为一句话 |
| 11 | 第471-477行 | 精简 | 2.4.5工程实现要点精简 |
| 12 | 第595行后 | 新增 | 系统总体用例图（见一-3） |
| 13 | 第598行 | 修改图注 | 功能模块图图注加突出说明（见三-32） |
| 14 | 第627行后 | 新增 | 协同编辑核心用例图（见一-4） |
| 15 | 第721行后 | 新增 | AI辅助用例图（见三-23） |
| 16 | 第878-886行 | 重画 | 核心领域类图替换现有图3-3（见一-5） |
| 17 | 第897行后 | 新增内容 | 3.4节补充协同延迟、AI超时、WebSocket重连指标（见四） |
| 18 | 第954行 | 新增文字 | DFD与UML区分说明（见三-28） |
| 19 | 第1040行 | 扩展表格 | 表4-2增加测试用例列和推荐系统行（见二-20） |
| 20 | 第1065行后 | 新增 | 部署架构图（见二-12） |
| 21 | 第1076行 | 修改说明 | 协同编辑时序图增加异常分支（见三-25） |
| 22 | 第1081-1131行 | 重画 | 系统E-R图重画为四条数据链（见一-6） |
| 23 | 第1131-1970行 | 补充说明 | 数据库各表补充外键、索引、唯一约束（见一-7） |
| 24 | 第2248行后 | 新增小节 | 5.2.7协作成员权限校验 + 5.2.8版本快照保存与回滚（见一-10） |
| 25 | 第2382行后 | 新增小节 | 5.3.5 AI调用日志记录 + 5.3.6 SSE中断与重试处理（见一-10） |
| 26 | 第2254行后 | 新增 | AI辅助双路径时序图（见二-13） |
| 27 | 第2151行后 | 新增 | 协同编辑活动图（见二-14） |
| 28 | 第2382行后 | 新增 | AI辅助活动图（见二-15） |
| 29 | 第2631行前 | 新增 | 需求—功能模块—测试用例对应表（见二-16） |
| 30 | 第2866行后 | 补充用例 | 协同编辑专项测试增加CT-13至CT-16（见一-8） |
| 31 | 第2865行后 | 补充用例 | AI测试增加AI-16至AI-19（见二-18） |
| 32 | 第3157行 | 修改标题 | 目录对应调整 |
| 33 | 第3158-3168行 | 重写 | 7.1工作总结按五维度重写（见二-19） |
| 34 | 第3174行后 | 新增内容 | 7.2增加第5点不足（API Key安全）（见二-19） |
| 35 | 第3210行后 | 新增内容 | 7.3增加第5点改进方向（移动端）（见二-19） |
| 36 | 第3211行(参考文献) | 修改标题 | 目录对应调整 |
| 37 | 全文 | 检查 | 图号、表号、图名、正文引用一致性（见一-11） |

---

## 六、修改过程中的注意事项

1. **口语化表达保留**：摘要中的"鸡肋""踩了一遍完整的坑""我们做了"等口语化表达，以及正文中"说白了""挺明确""没到毫秒级那种程度"等表达，全部保留不做修改——这是控制AIGC检测率的重要手段。审查意见中关于"语言学术化"的要求，仅在不影响AIGC率的前提下做最小调整，例如将"我们"偶尔换成系统/本项目，但不要通篇改写。

2. **新增的修改内容也注意口语化**：上述新增的文字（如工作总结、不足描述、图注说明等），在写的时候也保持一定的口语化风格——不要写成完美的学术化语言，用一些"大家""基本上""说白了"这类表达，避免新写的部分AIGC检测率过高。

3. **图表绘制规范**：新增的用例图、类图、活动图、部署架构图等应采用UML 2.x标准符号。用例图用椭圆表示用例、人形图标表示Actor；类图用三层矩形（类名/属性/方法）；活动图用圆角矩形、菱形分支、泳道分区；部署架构图用立方体表示节点。

4. **编号调整**：新增图表后，后续所有图表编号顺延。例如在3.2节新增2张图后，原图3-1→3-3变为图3-3→3-5，后续所有图号+2。建议在新增大批量内容后，全文重新编排图号表号并更新目录。

5. **参考文献**：审查意见中提到需要核查格式并补充CRDT/Yjs、WebSocket、AI写作辅助相关权威文献。当前参考文献中已有CRDT相关文献（吕晓等）、云笔记相关文献，但缺少Yjs官方文献、y-websocket相关文献。建议补充1-2篇WebSocket/协同编辑相关的近年文献。
